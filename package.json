{
  "name": "filmsonfreeview-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "nodemon --exec 'nf start'",
    "db:create-db": "db-migrate db:create filmsonfreeview",
    "db:drop-db": "db-migrate db:drop filmsonfreeview",
    "db:create-migration": "bash -c 'db-migrate create $0 --sql-file'",
    "db:up": "db-migrate up",
    "db:down": "db-migrate down",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "db-migrate": "^0.10.0-beta.24",
    "db-migrate-pg": "^0.2.5",
    "express": "^4.16.2",
    "postgraphql": "^3.5.0"
  },
  "devDependencies": {
    "eslint": "^4.8.0",
    "foreman": "^2.0.0",
    "nodemon": "^1.12.1"
  }
}
